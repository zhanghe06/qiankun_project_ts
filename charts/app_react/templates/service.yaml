apiVersion: v1
kind: Service
metadata:
  name: {{ include "app_react.fullname" . }}
  labels:
    {{- include "app_react.labels" . | nindent 4 }}
spec:
  type: {{ .Values.service.type }}
  ports:
    - port: {{ .Values.service.port }}
      targetPort: http
      protocol: TCP
      name: http
  selector:
    {{- include "app_react.selectorLabels" . | nindent 4 }}
